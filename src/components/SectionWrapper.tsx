// components/SectionWrapper.tsx
const SectionWrapper = ({ children }: { children: React.ReactNode }) => {
    return <div className="mb-12 md:mb-32">{children}</div>;
  };
  
  export default SectionWrapper;
  